lemisoft_sylius_abandoned_cart_plugin_shop:
    resource: "@LemisoftSyliusAbandonedCartPlugin/config/shop_routing.yml"
    prefix: /{_locale}
    requirements:
        _locale: ^[a-z]{2}(?:_[A-Z]{2})?$

lemisoft_sylius_abandoned_cart_plugin_admin:
    resource: "@LemisoftSyliusAbandonedCartPlugin/config/admin_routing.yml"
    prefix: /admin
